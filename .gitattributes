# Shell scripts run inside the Linux Dev Container, where a CRLF shebang fails
# with "bash\r: No such file or directory". Keep them LF in the working tree on
# every platform, regardless of core.autocrlf.
*.sh text eol=lf
