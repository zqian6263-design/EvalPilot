fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
# OpenAI-compatible chat-completions client for the optional live-LLM mode.
# The deterministic default never makes an outbound request.
httpx>=0.27
# Public open-source retrieval engine used by the HTTP SUT acceptance adapter.
haystack-ai>=3.1,<4

# dev / test
pytest>=8.0
# Standard JSON Schema library. Validates the published workload authoring
# contract (schemas/workload.schema.json) in scripts/validate-sut.ps1 and in the
# tests that keep the schema and the real workloads in sync. Nothing in the
# evaluation engine imports it.
jsonschema>=4.21

# Optional, policy-gated browser executor; no browser download is performed by pip.
playwright>=1.50
